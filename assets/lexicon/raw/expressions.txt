% Expressions %

@expr(dybram dinam) @link(dybros, dina) @def($hello, $good $morning, $good $day, \par{formal greeting used while the sun is up})
@expr(dybrom veksperom) @link(dybros, veksperos) @def($hello, $good $evening, \par{formal greeting used after sunset})
@expr(dybram noptym) @link(dybros, nopts) @def($good $night, \par{formal or informal term used right before going to sleep})
@expr(megti esni) @link(megmi, esmi) @def($maybe, $perhaps, ${could be})
@expr(jodi siet) @link(jodi, esmi) @def($wherever, $anywhere)
@expr(joda siet) @link(joda, esmi) @def($whenever, $anytime)
@expr(tasme dybre) @link(tos, dybros) @def($too, $also; $moreover, $additionally)
@expr(pasme) @link(pos) @def($how, \par{interrogative})
@expr(jasme) @link(jos) @def($how, \par{relative})
@expr(tasme) @link(tos) @def($like $that)
@expr(sjasme) @link(sjos) @def($like $this)
@expr(inopasme) @link(inopos) @def($somehow)
@expr(do visties) @link(do, vistis) @def(${so long}, $goodbye, ${see you later}, ${until next time}, \par{formal term used when parting from someone when expecting to see them again})
@expr(pro pod?) @link(pro, pos) @def($why?)
@expr(pro jod) @link(pro, jos) @def($because)
@expr(apo tosmod) @link(apo, tos) @def($therefore, $thus, $hence, $because of that, $consequently)
@expr(pro tod) @link(pro, tos) @def($therefore, $thus, $hence, $because of that, $consequently)
@expr(prinom jerom) @link(prinos, jeros) @def($happy ${new year})
@expr(prinam dinam djynties) @link(pinos, dina, djentis) @def($happy $birthday)
@expr(dina djynties) @link(dina, djentis) @def($birthday)
@expr(didoni vidos) @link(didomi, vidos) @def(to $seem, to $appear, to give the $appearance of)
@expr(djystre) @link(djesor) @def(\par{used after an adjective in the instrumental case to make an adverb}; \ex{krytue djystre}{strongly}, \ex{pryme djystre}{firstly}, \ex{finale djystre}{finally}, \ex{apparente djystre}{apparently})
@expr(reponte djystre) @link(repo, djesor) @def($suddenly, &sudden)
